# Vibeco2 — Decisions Log

Non-obvious choices and why, not a session-by-session changelog. Full history remains in `git log` — this file only tracks what's still load-bearing.

## Current shape (as of 2026-08-23)

- **Tabs:** Home / Cowork / Solo / Preview. Canvas is disabled (button removed, code intact, not deleted).
- **Cowork** (`AgentWindow.tsx`) = team attention mode: shared draft editor + ready-check, plus its own "Add to Queue" button.
- **Solo** (`ChatPane.tsx`) = heads-down individual chat, no ceremony to send — plus its own "Add to Queue" button, same queue as Cowork's.
- **The queue (`ShelfPanel`, UI label "Merge queue") is app-wide, not per-mode or per-chat, and is the only path to `team` anywhere in the app** — Chat's old instant-push button and the canvas node's "Promote to main" were both removed (see decision 3 below). "Add to Queue" (in each surface's own chat header) commits + pushes a chat branch and gets it a real Claude-written summary of its diff against `team`; "Merge to Team" merges everything queued into `team`, unconditionally, one item at a time. `team` → `main` (review/approve in the Preview tab) is a separate, not-yet-built step — see decision 3 and the Known open work section.
- **The queue is durable and live-shared** (`queue_items` table, `supabase/migrations/0025_queue_items.sql`), project-scoped, same open-to-authenticated RLS pattern as the rest of this project. Was pure client `useState` until 2026-08-24 — a queued item vanished on app restart with no recovery path short of re-diffing and re-queueing by hand, and wasn't visible to a teammate on a different machine at all. `App.tsx` fetches on `project.id` change and subscribes to INSERT/UPDATE/DELETE on the existing `messages-live` realtime channel (same optimistic-add-then-dedupe-the-echo pattern as `preview_strokes`/`preview_pins`).
- **Home** = task-coordination dashboard (task lists = `chat.group_name`, completion = archived, assignment = claimant/handed-off-to), plus a trimmed "At a glance" section (your mentions/handoffs + recent activity). Deliberately not activity-log-shaped — see decision 6.
- **Multi-project:** `projects` table keyed by `repo_url` (not a local path — see decision 5), clone-on-demand per teammate. Chats, the Liveblocks room, and the git worktree root are all project-scoped.
- **Messages** carry `authorEmail` (who sent it) — drives the mention/@ system and per-teammate name labels in multi-author chats. (No longer drives a shelf approver list — see decision 3/4.)

## Standing decisions (with rejected alternatives)

1. **Liveblocks, not Supabase Realtime, for multiplayer.** Supabase stays the durable Postgres/Auth layer; Liveblocks is the live layer (presence, cursors, CRDT storage). Liveblocks is purpose-built for this (ready-made cursor/awareness components, conflict-free storage) and already proven in the sibling VibeCo codebase. Supabase Realtime was rejected as the sole backend — no CRDT layer, would need hand-rolled conflict resolution for concurrent edits.

2. **Session handoff via transcript-priming, not shared native resume.** `claude --resume` only works for the machine/account that created the session, but chats are a shared claimable resource. When the sender owns the session: normal native `--resume`. When they don't: skip resume, prepend a serialized transcript preamble instead, then transfer ownership. Rejected: always-inject (pays the cost even for the common same-owner case) and no-handoff-support (defeats the point of shared chats). Cost: the first message after a handoff is pricier and tool results get summarized, not replayed — accepted tradeoff.

3. **Queue → publish (to `team`) is one shared, unconditional pipeline; `team` → `main` is a separate, not-yet-built approval step.** Went through two wrong shapes before landing here:
   - *First shape:* shelf/publish was Cowork-only (Solo instant-pushed straight to `team`/`main`), reasoning that Cowork's ready-check means "everyone affected is present" so publish should need their agreement, while Solo work only touches your own worktree. Corrected same-day (2026-08-23) — if Cowork changes go through review and Solo changes never do, uncoordinated solo pushes could still land mid-merge with no way to time or review them. Fixed by giving Solo its own "Add to Queue" button into the same app-wide queue.
   - *Second shape (same day):* "Add to Queue" itself immediately merged the chat branch into `team` and pushed it — queueing and shipping-to-team were the same moment, and a per-item "everyone who contributed must agree" toggle in the shelf gated a *second* button that pushed `team` → `main`. This conflated two different decisions (should this land in team? should team go to main?) into one queue UI, and buried real git-merge risk (a conflict) inside what was framed as just "queueing."
   - **Current shape:** Add to Queue only commits + pushes the chat branch (`commit_and_push_chat_branch`) — `team` is untouched. Publish takes everything queued and merges each into `team` (`merge_chat_into_team`, run once per item, unconditionally — no approval gate). A per-item conflict is surfaced and left in the queue rather than blocking the rest of the batch. Reviewing/approving before `team` merges into `main` still needs building — intended to live in the Preview tab (visual review, "does it look right, any bugs"), gated on approval from "everyone applicable," not the old per-shelf-item agree toggle (removed). `promote_to_main` (Rust) still exists and does exactly that merge; nothing in the UI calls it right now.

4. ~~Shelf approvers = message authors in that chat, not current room occupants.~~ **Superseded by decision 3's current shape** — there's no approval gate on the queue/publish step anymore. This exact idea (approval scoped to contributors, not room occupants) is worth reusing once the `team` → `main` approval step above gets built.

5. **Projects store `repo_url` + clone-on-demand, not a local filesystem path.** `projects` is one shared row per project across the team — a local path only works if every teammate's checkout happens to live at an identical path, which won't hold. Each client clones into `<app_data_dir>/projects/<project_id>` on first open, deriving the same relative answer independently — no path bookkeeping, shared or per-machine. No GitHub OAuth/repo-browsing yet; assumes the same git credentials (SSH key/credential helper) already configured locally, same assumption the render/promote push code already made.

6. **Home is task-coordination-shaped, not activity-log-shaped**, after an explicit reframe mid-build. A trimmed "for you" mentions/activity section was later folded back in as one section among the task-coordination ones — compatible with the reframe since it's scoped to you, not a full team audit log. If Home starts feeling log-heavy again, that's the section to cut first.

7. **Human-only comments (no Claude invocation) were discussed and rejected/deferred.** Bolting a `to_claude` flag onto the existing message stream would merge human-coordination and AI-directing into one entangled model. If built later, it should be the dedicated separate human-chat column the original spec called for, not a mode-switch on the Claude-turn stream. What stays regardless: @mentions, handoff/assignment, and the per-chat "open" toggle (lets a non-claimant post) — none of these depend on the merged-stream idea.

8. **Mentions are persisted DB rows, not fire-and-forget Liveblocks broadcasts.** A broadcast is invisible to anyone offline at the instant it fires — the actual bar is "tag someone, they see it whenever they next open the app." One row per recipient (`@all` expands to one row per teammate) so read state stays per-person. Handoffs reuse the same table/pipeline (`kind: 'mention' | 'handoff'`) rather than a second notification system. Explicitly out of scope: reaching someone while the app itself isn't running (push notifications, waking a sleeping machine).

9. **Tailwind v4 + Radix/shadcn adopted** over MUI/Chakra/Ant Design (migration completed 2026-08-19/20) — those impose their own visual identity, which fights this app's existing dark palette. Durable gotcha from that migration, worth remembering on any future styling work: **Tailwind compiles utility classes in its own internal order, not JSX class-string order.** Two same-specificity classes targeting the same CSS property (e.g. a `base` and an `active` string both setting `background-color`) resolve by compiled source order, which is opaque and not stable to reason about from the JSX. Rule: never let more than one of a `base`/`active`/`inactive`/`disabled`-style class string touch the same property on the same element.

10. **Real token-level streaming**, not synthetic typewriter. The CLI's `--include-partial-messages` stream was already being requested but the parser ignored `stream_event`/`content_block_delta` entirely, so all text arrived as one lump per finished block regardless of generation speed. Fixed to consume real deltas, then added a client-side smoothed-reveal layer (rAF trickle at ~260 chars/sec, catches up if backlog exceeds 120 chars, snaps to full text the instant streaming ends) purely to smooth the CLI's bursty ~10-15-char delta chunks — never invents delay for text that hasn't arrived yet.

11. **Never run a scaffolding tool with a force/overwrite flag inside a directory with files worth keeping.** Incident (2026-08-04): `create-tauri-app --force` wiped `spec.md`/`decisions.md`/`docs/` because `--force` means wipe-and-recreate, not "tolerate extra files." Recovered from conversation context only, no commit existed yet. Scaffold into a fresh temp directory and move generated files in, or commit existing docs first so there's always a git-recoverable baseline.

12. **Restricted-chat access (`chat.open === false`) is gated in `lib/chatLock.ts`, keyed off `claude_session_owner` (falling back to `user_id`) — not live Liveblocks presence, and not a uniform on/off switch.** Found via a real cross-account test: a chat someone was "working on solo" and had explicitly restricted was still editable by a teammate switching to Cowork. Root cause — a chat's live claimant (`self.presence.claimedChatId`) is only set at the moment of `handleSend` and clears on reload, so the old gate (`claimedByOther && !chat.open`) treated "restricted + no live claimant" as open to everyone. The persisted owner field survives reloads and fixes that, but the rule ended up with three parts, not one:
   - **Cowork is all-or-nothing per chat**, no owner exception: `isChatLockedForCowork` blocks *everyone*, including the owner, while the restriction is in effect — Cowork is a team surface, so a restricted chat is entirely off it (not selectable in the sidebar, not offered by either "choose a chat" picker, and the pane shows an explicit locked message if you land on it via the tab switch) until explicitly unlocked.
   - **Solo is owner-edit / everyone-else-observe**: `isChatLockedForViewer` always exempts the owner (so they can keep working normally) but disables sending for anyone else, leaving the chat open to read.
   - **Auto-unlock on idle/offline**: both functions treat the restriction as inactive — i.e. unlocked — once the owner isn't around to be interrupted by: absent from `onlineEmails` (offline), or present but `last_message_at` is older than `IDLE_MS` (10 minutes, online-but-idle). This uses fields that already existed (`last_message_at`, presence) rather than adding new tracking.
   Sidebar's lock icon and its Cowork-row click-block both call `isChatLockedForCowork` directly, so they reflect the same live/idle/offline state as the actual gate rather than the raw `chat.open` bit.

13. **Cowork's shared draft is real concurrent multiplayer editing (Yjs CRDT via Tiptap + `@liveblocks/yjs`), not a turn-taking lock.** Went through two smaller shapes first, both explicitly requested as the fast version at the time: presence-only cursors with a local-per-user draft, then a last-focus-wins lock (mirroring Solo's `InputBar.tsx` typing indicator) that mirrored the holder's live text/HTML to everyone else read-only. The lock was rejected once actually used — "only one person can type at once" wasn't what was wanted; the ask was real Google-Docs-style concurrent editing. That needs a CRDT, not presence broadcasting, so the editor itself was swapped from a hand-rolled `contentEditable` (execCommand + manual Range-API list/quote wrapping, ~250 lines) to Tiptap (`@tiptap/react`, `@tiptap/starter-kit`) bound to a Yjs document via `@tiptap/extension-collaboration`. One Y.Doc per project room — `getYjsProviderForRoom(room)` from `@liveblocks/yjs` is a room-keyed singleton, so it doesn't need its own create/destroy lifecycle — with one Yjs `XmlFragment` per chat (`field: draft-${chatId}`), so different chats don't share a document. Live colored carets come from `@tiptap/extension-collaboration-caret` (renamed from `-collaboration-cursor` in Tiptap v3). Formatting (bold/italic/underline/lists/quote) moved from execCommand/manual DOM surgery to Tiptap's built-in toggle commands and input rules — a large net deletion, not just a swap, since the library already correctly handles what the hand-rolled version was working around (WebKit's unreliable `execCommand` list/formatBlock support). Sending still clears the draft via `editor.commands.clearContent(true)` — since the draft is now one shared document, this clears it for every collaborator, not just the sender, which is the only sensible behavior once it's genuinely shared. Accepted trade: exact keyboard-behavior parity with the old hand-rolled editor (e.g. Enter explicitly exiting a blockquote) was dropped in favor of Tiptap's stock behavior — not reimplemented, since re-deriving custom keymap behavior on top of a real editor library would reintroduce the same class of bug the migration was meant to remove.

14. **Team preview is, and stays, a local dev server per machine — not something a teammate's merge pushes to. It syncs on manual pull, not automatically.** Found via a real cross-machine test: Josh merges to `team`, Ben's Preview tab (Team mode) never changes. Root cause is structural, not a bug — `ensure_team_worktree` (`git_ops.rs`) only fetches/checks out `team` the *first* time a machine creates that worktree; nothing re-fetches it afterward, and `TEAM_PREVIEW_URL` (`http://localhost:5180`) is explicitly bound to `127.0.0.1`, so it was never going to be reachable across machines even if it did stay in sync — Team preview means "my machine's own server serving my machine's own `team` checkout," full stop, for every person. Considered and rejected: auto-pull on every Preview-tab view/switch (explicitly rejected — "sometimes you don't want it to autoupdate on every tab change, that'd get tiresome") and a bare manual-refresh button with no staleness signal (not enough on its own — people won't remember to click it if nothing indicates there's something to pull). Landed on: poll `team_preview_has_update` (a bare `git fetch origin team` + local-vs-`origin/team` SHA compare, cheap since it moves no file content) every 20s while Team mode is active, and light up the existing Update button (dot + tinted background) when it's true. Clicking it while lit calls `pull_team_preview_update` (`git fetch` + `reset --hard origin/team` in the local `team` worktree — safe as a hard reset for the same reason `merge_chat_into_team`'s own resync is: this worktree only ever holds already-pushed merges, never independent local work) before bumping the iframe's reload key. No server restart needed either way — `TeamPreviewServer` is one long-lived `npm run dev` per machine (see its own comment in `preview_server.rs`) already watching the worktree directory, so Vite's file watcher picks up the pulled files and hot-reloads on its own; the reload-key bump is just to force a clean iframe reload on top of that, same as the button already did for the local-preview case.

## Session hand-off (2026-08-23, latest)

**What shipped:** condensed this file from 977 lines to its current form; removed the dead "Projects" sidebar row; reworked the queue/publish pipeline per decision 3 (queue now only commits+pushes a chat branch and gets an AI diff summary, no team merge until "Merge to Team" is clicked; Solo got its own queue button matching Cowork's); moved the queue button into each chat's header (ordered Add to Queue / Preview / ⋯, right-aligned, styled as a low-opacity tint of the user's own presence color) and moved the open/lock toggle to the header's left side next to Assign; renamed the panel's UI copy throughout (Shelf → Merge queue, Publish → Merge to Team, dropped the stale "latest reply" phrasing).

**Current state:** `main` is one commit ahead of `origin/main` (`d8bfec8`, the decisions.md condense, from earlier this session) plus everything above, all uncommitted until this hand-off's own commit. `npx tsc --noEmit`, `npx vitest run` (89/89), and `cargo test` (28/28) all clean. Not clicked through live in `tauri dev` this session (per standing practice — user verifies UI directly).

**Note on `Sidebar.tsx`/`InputBar.tsx`/`src/lib/chatLock.ts`:** these were already modified/untracked on disk before this session touched them — a complete, tested (`chatLock.test.ts`, 8/8) implementation of decision 12's restricted-chat lock icon and an `InputBar` placeholder prop. Not this session's work, but real and finished, so included in the same commit rather than left stranded.

**Open items:** none from this session's own requests — everything asked for landed. The one thing still explicitly deferred (not a gap, a stated decision) is `team` → `main` review/approval in the Preview tab — see decision 3.

**Recommendation:** safe to start fresh next session once this is committed/pushed.

## Session hand-off (2026-08-24)

**What shipped:** a long debugging chain that started as "preview tab button styling" and ended up hardening the entire git worktree/queue/merge pipeline against a real fresh-GitHub-repo project, plus a preview-comments fix and two small features:

- **Cowork instant-lock bug (fixed):** `chats.open` defaulted `false`; decision 12 had repurposed `false` into a hard Cowork lock with no owner exception, so every new chat locked itself out the moment its first message bumped `last_message_at`. New chats now default `open: true` (migration `0021`, plus a code default).
- **Project repo switching (fixed):** editing a project's GitHub URL used to just `remote set-url` an already-cloned directory, silently leaving all the *old* repo's files/branches/worktrees in place. Now detects the origin mismatch and re-clones. Worktrees are also now scoped per-project (`<project-id>-worktrees`) instead of one folder shared by every project on the machine.
- **Git pipeline self-healing (fixed):** chat/team worktrees relocate instead of erroring if their expected path changes; a brand-new empty GitHub repo gets bootstrapped with a real initial commit (seeding the preview-tracker script, see below) instead of assuming `main` exists; the repo's actual default branch is resolved instead of hardcoding `"main"`; `merge_chat_into_team` no longer hard-fails on a `team` branch that hasn't reached origin yet (first merge on a new project).
- **Silent-failure landmines (fixed):** removed a fallback that could silently run a Claude session with the wrong working directory instead of failing loudly (this was the actual root cause of "merge produced zero files" earlier in the session); the unmerged-work check gating chat deletion now fails toward "assume unsafe" instead of "assume safe" (both the Rust and frontend layers had the same bug).
- **Preview server (fixed):** added a static-file fallback (Python's `http.server`) for projects with no `package.json`; fixed the same PATH-resolution issue for `python3` that `npm` already had a fix for; added a port-open poll before reporting "ready" so the iframe stops racing a server that hasn't started listening yet (this was why Local previews showed a blank screen with no error).
- **Preview comments (fixed):** `created_by` on all three preview annotation tables had no `default auth.uid()` (every other such column in this project does) — every pin/reply/stroke insert had *always* silently failed a NOT NULL constraint since the feature shipped. Migration `0022` fixes it; failures now also surface a toast instead of only logging.
- **New: draggable pins.** Click-vs-drag distinguished by a 4px movement threshold; optimistic local update so it doesn't snap back waiting on the network.
- **New: page-scoped comments.** `vibeco-preview-tracker.js` is now auto-seeded into every new project's first commit and Claude is told (system-prompt guardrail) to wire it into pages it builds; it `postMessage`s the current path to the app (the iframe is cross-origin, so this is the only way to know what page is showing), and pins are filtered to the current page (unscoped/legacy pins always show). Migration `0023` adds `preview_pins.page_path`.
- **Also:** Preview tab's Team/Local toggle restyled to match the app's real tab component; chat-select sizes to content instead of a fixed width; Cowork draft editor's oversized line-height fixed; desktop/mobile viewport toggle added with an iPhone-style frame.

**Current state:** everything above is committed and pushed (`f39b436`). `npx tsc --noEmit`, `npx vitest run` (91/91), and `cargo test` (29/29) all clean. Not clicked through live in `tauri dev` this session (per standing practice — user verified UI directly, iteratively, across the whole session).

**Open items:**
- Three Supabase migrations (`0021`, `0022`, `0023`) need running against the live Supabase project — the user ran `0021` mid-session but should double check `0022`/`0023` also landed.
- Page-scoped comments only auto-apply to **new** projects (the tracker script is seeded on first commit). An existing project created before this session won't have it — explicitly flagged to the user as a follow-up if they want it retrofitted.
- The two known bugs below (Cowork toolbar caret/hover, timestamp hover-to-fade) are untouched this session — still open.

**Recommendation:** safe to start fresh next session. Suggest confirming the two remaining migrations are applied before doing more Preview-tab work.

## Session hand-off (2026-08-24, evening)

**What shipped:** two Preview-tab fixes plus one small feature, all committed and pushed (`c4f7f98`):

- **Preview comments — delete + in-place popover (feature).** Clicking a pin now opens a Figma-style card anchored at the pin itself (text, replies, reply box, Resolve, Delete, close ×) instead of only opening the sidebar list; a Delete button was also added to the sidebar list. New `preview_pins_delete_all` RLS policy (migration `0024`) — deleting a pin cascades to its replies via the existing FK. Realtime DELETE handling added for both `preview_pins` and `preview_pin_replies`.
- **Directory-listing preview bug (fixed).** Python's static-file `http.server` fallback (used when a project has no `package.json`) only auto-serves a file named exactly `index.html` — a project whose entry file is named something else (e.g. `padel.html`) showed a raw directory listing instead of the page, and the user had to click through it manually. `spawn_dev_server` (`preview_server.rs`) now detects the real `.html` entry file and 302-redirects `/` to it.
- **Update button (feature).** Added a reload button, top-left of the preview pane, styled like the app's real tab component — remounts the iframe (via a `key` bump) since a plain cross-origin `.reload()` call isn't possible. Also made the pane's three floating pill backgrounds (Update, Team/Local, Desktop/Mobile) solid instead of 40%-opacity, which washed out against light-colored previews.
- **Unrelated build-breaker (fixed in passing):** `src-tauri/tauri.conf.json` had picked up a `mainBinaryName` field (added outside this session, by another concurrent session or the user) that the installed `tauri-build` crate (`2.6.3`, the latest published on crates.io) doesn't recognize — `cargo` rejected it outright and the whole dev build was down. Removed the field; then renamed the Cargo package itself (`src-tauri/Cargo.toml`, `vibeco` → `Vibeco`) so the unbundled `tauri dev` binary — and therefore what macOS's Dock/`System Events` show for the running process — reads "Vibeco" instead of lowercase. Confirmed via `osascript`/System Events that the OS now reports the process as "Vibeco". User still saw lowercase in their own Dock hover afterward and said not to worry about it further — likely a stale pinned Dock icon from an earlier lowercase run, not something in the code; **left open, not fixed**.
- Orphaned dev-server processes came up twice this session (a killed Tauri app doesn't clean up its spawned `npm run dev`/`python http.server` children, so they keep squatting fixed ports like `5180` and mask any fix until manually killed) — worth a proper fix (kill child on `TeamPreviewServer`/`ChatPreviewServers` drop, or track+kill by port) if this keeps recurring.

**Note on scope:** this session's own edits were the four points above. The working tree also had substantial unrelated in-progress work from a concurrent session (queue items becoming a durable live-shared table — `queueItems.ts`, migration `0025`, `PillToggle.tsx`, `App.tsx`/`AgentWindow.tsx`/`ShelfPanel.tsx` changes) already sitting uncommitted when this session ran `git commit`. Per the user's explicit "commit, push" request everything was committed together in one commit rather than split, since the changes were interleaved in some of the same files (`PreviewCommentPanel.tsx`, `previewComments.ts`, `PreviewPage.tsx`) and couldn't be cleanly separated after the fact.

**Current state:** `main` is committed and pushed, nothing outstanding (`git status` clean, `git log @{upstream}..` empty). `npx tsc --noEmit` and `cargo check` both clean.

**Open items:**
- ~~Migration `0025` (queue-items)... `0021`–`0024` are still not applied...~~ **Resolved (2026-08-24, later still):** all of `0021`–`0025` are now applied to the live Supabase project (`febfuemspzwslaujdtwc`). User ran `0021`–`0023` as one batch in the SQL editor, which silently stopped partway (`0023`'s `page_path` column already existed from an earlier partial run, erroring out the rest of the batch before `0024`'s delete policy could run) — caught by directly querying `information_schema`/`pg_policies` rather than trusting the batch's "success," then `0024` was re-run alone. Verified live: `chats.open` defaults `true`, all three preview tables' `created_by` default `auth.uid()`, `preview_pins.page_path` exists, `preview_pins_delete_all` policy exists. No pending migrations remain.
- The lowercase-Dock-hover report above is unresolved and explicitly deprioritized by the user.
- Orphaned preview-server child processes on force-kill, noted above, not fixed.
- **Addendum — the "concurrent session" work this hand-off refers to (queue items, `PillToggle.tsx`, etc.) was this session, not a separate one; the same session also shipped, earlier in the day and already included in `c4f7f98`:** AI-inferred chat titles (`generate_chat_title` Rust command, upgrades the instant heuristic title once Claude replies); a Preview-tab draw-tool bug where a stroke vanished on pointer-up (now added to local state optimistically, same pattern as pin-drag) plus a new Clear-all-drawings toolbar button; Home tab gained a "Page in Progress" title + roadmap blurb; Cowork's quote button now toggles off instead of infinitely nesting blockquotes (mirrors the existing list toggle-off logic), and its placeholder text's mismatched line-height (was 2.2 vs the editor's 1.4, causing visible misalignment while typing) is fixed and the copy shortened; the Solo pane's "Add to Queue" button text is now white.

**Recommendation:** safe to start fresh next session — all migrations through `0025` are confirmed live, nothing pending.

## Known open bugs

- **Cowork toolbar (`AgentWindow.tsx`): caret invisible next to list markers, and a stuck grey hover box on toolbar buttons — unresolved despite two rounds of fixes that were each individually verified.** Both fixes were validated via out-of-app standalone HTML/JS reproductions (this session can't sign into the app to test live) and the DOM-position/hover-state reasoning held up in isolation — but the user confirmed both are still broken in the real running app. Don't trust an out-of-app repro's verdict here again; next attempt needs the user driving the actual app or a debugger attached to the live signed-in session.
- **Message timestamp hover-to-fade doesn't hide in the real Tauri app**, despite three different implementations (CSS `:hover`, per-row React state, document-level `mousemove`) all confirmed correct against an isolated repro served from the same dev server. Leading unconfirmed hypothesis: WKWebView's persistent bundle-identifier-scoped cache may be serving a stale JS bundle across restarts. Paused by user request rather than fixed — next step is clearing `~/Library/WebKit/<bundle-id>` and `~/Library/Caches/<bundle-id>`, or attaching Safari's Web Inspector to the running window.

## Deliberately out of scope (raised, not built)

- GitHub OAuth / browsing a teammate's repos in a dropdown for project selection.
- Live collaborative (Google-Docs-style) editing of Cowork's shared draft — would need the draft moved into Liveblocks `Storage` (Yjs), live cursors, and shared toolbar/formatting state. Scoped but not started; a separate piece of work from anything else here.
- Windows support for the permission-approval bridge — it's a Unix domain socket (`permission_bridge.rs`), matching this codebase's existing macOS-only assumptions elsewhere.
- Real auto-update for a packaged `.app` build — the self-update button only works under `tauri dev` (relies on its own file watcher for the "recompile" step).
